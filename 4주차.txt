6장
RNN 문제점

기울기 폭발
해결책으로 기울기 클리핑 (gradients clipping)

기울기 소실

은닉 상태 h(t)는 기억 셀 c(t)에 단순히 tanh 함수를 적용했을 뿐

tanh(c(t))에 게이트를 적용
-> tanh(c(t))의 각 원소에 대해 '그것이 다음 시각의 은닉 상태에 얼마나 중요한가'
-> 이 게이트는 다음 은닉 상태 h(t)의 출력을 담당하는 게이트이므로 output게이트 (출력게이트)라고 함

p.292 ~ 293 결정적, 확률적의 차이를 모르겠음

p.294~295 코드부분
' skip_ids 는 단어 ID의 리스트인데(예컨대 [12,20]), 이 리스트에 속하는 단어 ID는 샘플링되지 않도록 해줍니다.
이 인수는 PTB 데이터셋에 있는 <unk>나 N 등, 전처리된 단어를 샘플링하지 않게 하는 용도로 사용합니다. '
라는 뜻이 이해가 안감